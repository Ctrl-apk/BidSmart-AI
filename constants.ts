
import { RFP, SKU } from './types';

// Empty initial states - data must be loaded via Admin Panel or Sales Console (AI)
export const SAMPLE_SKUS: SKU[] = [];

export const INITIAL_RFPS: RFP[] = [];

// Removed MOCK_PRODUCT_EXTRACTION and MOCK_TEST_EXTRACTION to force dynamic AI extraction
